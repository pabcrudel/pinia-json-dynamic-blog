# Auto detect text files and perform LF normalization
* text=auto

# Denote files that are truly binary and should not be modified by Git
*.jpg binary
*.png binary
*.gif binary
*.ico binary

# Denote files that are text but should still use CRLF line endings on checkout
*.txt text eol=crlf
